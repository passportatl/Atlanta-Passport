# Atlanta Passport

A premium, mobile-first marketing site for "Atlanta Passport" — a hyperlocal city guide (physical booklet + digital site) selling sponsorship/listing packages to Atlanta businesses ahead of the 2026 World Cup. Wheelhaus Bikes is the founding sponsor and showcase listing.

## Run & Operate

- `pnpm --filter @workspace/api-server run dev` — run the API server (port 5000)
- `pnpm run typecheck` — full typecheck across all packages
- `pnpm run build` — typecheck + build all packages
- `pnpm --filter @workspace/api-spec run codegen` — regenerate API hooks and Zod schemas from the OpenAPI spec
- `pnpm --filter @workspace/db run push` — push DB schema changes (dev only)
- Required env: `DATABASE_URL` — Postgres connection string

## Stack

- pnpm workspaces, Node.js 24, TypeScript 5.9
- API: Express 5
- DB: PostgreSQL + Drizzle ORM
- Validation: Zod (`zod/v4`), `drizzle-zod`
- API codegen: Orval (from OpenAPI spec)
- Build: esbuild (CJS bundle)

## Where things live

- `artifacts/atlanta-passport/` — Atlanta Passport React + Vite frontend (the product)
- `artifacts/atlanta-passport/src/data/sample-data.ts` — businesses, events, neighborhoods, packages, categories, **exploreCategories**, **routes**, **beltlineStops**
- `artifacts/atlanta-passport/src/data/business-sheet-data.ts` — auto-generated content overrides (`description`/`about`/`offer`/`hours`/`address`) imported from the partner Google Sheet (BUSINESS tab), keyed by business `id`. `sample-data.ts` exports `businesses = businessesRaw.map(b => override ? {...b, ...override} : b)`, so the sheet is authoritative for those text fields while curated fields (lat/lng, image, transit, sponsorTier, categories, structured menu) are preserved. To refresh business copy from the sheet, regenerate this file — don't hand-edit those fields in `sample-data.ts`.
- `artifacts/atlanta-passport/src/components/Sticker.tsx` / `PassportStamp.tsx` — reusable brand primitives (color/rotation variants)
- `artifacts/atlanta-passport/src/index.css` — theme tokens, brand palette, all design-system classes
- `artifacts/api-server/` — shared Express API server (currently only `/api/healthz`)
- `lib/api-spec/openapi.yaml` — API contract source of truth

## Architecture decisions

- Marketing site uses static sample data in `src/data/`.
- Apply form is local-state only (no submit endpoint); shows a thank-you state after submit.
- **Digital Passport** is backend-wired (Postgres + Express). Visitor accounts, businesses, and stamp collection live in `@workspace/db`; API contract lives in `lib/api-spec/openapi.yaml`; React Query hooks come from `@workspace/api-client-react` (Orval codegen). Visitor id persists in `localStorage` (`atlanta-passport-visitor-id`) via `VisitorProvider`.
- **Account sign-up (Replit-managed Clerk)** — the "Start your passport" form (`src/passport/StartPassportForm.tsx`) is a real Clerk **email + password** sign-up with the email-verification-code step, plus social sign-up (Google + Apple + X + Facebook; Instagram intentionally dropped — Meta deprecated consumer Instagram Basic Display) below it. The form is two-step (`form` → `verify`): `signUp.create({emailAddress, password, unsafeMetadata:{firstName, phone}})` + `prepareEmailAddressVerification({strategy:"email_code"})`, then `attemptEmailAddressVerification({code})` and `setActive({session: createdSessionId})` (guarded on `status==="complete" && createdSessionId`). firstName/phone ride in `unsafeMetadata` because the Clerk instance has the name attribute OFF; `/api/visitors/link` reads them from `user.unsafeMetadata` (firstName fallback `user.firstName || meta.firstName || "Friend"`). It uses `useSignUp` from **`@clerk/react/legacy`** and includes a `<div id="clerk-captcha"/>` for bot protection. No manual navigation after sign-in — `ClerkVisitorBridge` links the visitor and `/passport` (and `/stamp/:slug` auto-collect) react to the new `visitorId`. All Clerk wiring lives in `src/auth/clerk.tsx`: `ClerkProviders` (wraps the app inside `WouterRouter`, routerPush/Replace strip the base path), branded `SignInPage`/`SignUpPage` (path-routed at `/sign-in`/`/sign-up`, handle the `…/sso-callback`), `SocialAuthButtons` (custom buttons with inline brand glyphs; uses `useSignUp` from **`@clerk/react/legacy`** — the v6 signals-based default `useSignUp` has no `authenticateWithRedirect`). `ClerkVisitorBridge` ties a Clerk account to the passport `visitor` record: while signed in it calls `POST /api/visitors/link` (get-or-create by `clerkUserId`) and makes that the active `visitorId` so the same account = same passport on every device; on sign-out/account-switch it drops the linked visitor (anonymous email-form visitors are left untouched). Server: `clerkMiddleware` + a Clerk proxy (`/api/__clerk`) in `api-server/src/app.ts`; `/api/visitors/link` derives identity from `getAuth(req)` (never client input). Schema: `visitors.clerkUserId text unique`. Secrets: `CLERK_SECRET_KEY`, `CLERK_PUBLISHABLE_KEY`, `VITE_CLERK_PUBLISHABLE_KEY`. **Provider enablement is a Clerk-dashboard step** — only Google is on by default; Apple/X need toggling and Facebook needs a Meta app (custom OAuth) for Production. Home `/` stays public (marketing site — no signed-in redirect).
- Routing via `wouter` with base path `import.meta.env.BASE_URL`. App.tsx splits routing by URL prefix: `/stamp/:slug` (full-bleed), `/admin/stamps` (admin), `/sign-in/*?` & `/sign-up/*?` (Clerk auth pages), the MapShell routes (see below — Router returns null, checked BEFORE the `/passport` branch), `/passport` (PassportLayout w/ bottom nav — profile home only), everything else (marketing Layout w/ Navbar+Footer). Marketing routes (`MarketingRoutes`): `/` (home), `/partners`, `/events/:id` (event detail), `/routes/:id` (route detail), `/apply`, `/listing/:id`, else NotFound.
- **Detail pages** follow one convention — a record selected from a list opens a dedicated detail page in the marketing layout: location detail `/listing/:id` (`pages/listing.tsx`), event detail `/events/:id` (`pages/event-detail.tsx`), route detail `/routes/:id` (`pages/route-detail.tsx`). `route-detail.tsx` mirrors event-detail's hero/body/sidebar shape: hero tinted by `route.color`, local Start/Time selectors (default `marta`/`noon`) driving `resolveRoute`, an ordered stop list (start anchor → legs → numbered stops linking to `/listing/:id`, with per-stop `~Xm` visit times), a sidebar (Plan Your Day selectors + "Open Routes Map" → `/passport/routes` + Get Passport), and prev/next route cards. It also renders a route-level **description** callout (after the vibe) and per-stop **Route Info** notes (Navigation icon) in the walk list — both read via safe-cast (`"description" in route ? … : ""` / `"stopNotes" in route ? … : {}`) since they're optional sheet-sourced fields. `RoutesFeed` cards carry a "View route details" link → `/routes/:id`.
- **Persistent MapShell** (`src/passport/MapShell.tsx`): the `/passport/explore`, `/passport/events`, `/passport/stamps`, and `/passport/routes` routes (`isMapShellRoute` in App.tsx) share ONE always-mounted shell so the Google map never reloads/recenters when switching between them. `PersistentMapShell` in App.tsx mounts it once on first visit and toggles `visibility` (not unmount) on a `fixed inset-0 z-30` container. MapShell owns the shared filter state (categories/neighborhood/search/selectedBizId/selectedRouteId) + the pinned `BusinessMap`, and swaps the content below the map by `view`: `/explore` → `ExploreContent` (`src/pages/explore.tsx`, presentational/props-driven — filters + results, no map/state), `/passport/events` → `EventsFeed` (`src/passport/EventsFeed.tsx`, event cards; clicking focuses the venue on the map, "View event" → `/events/:id` marketing detail), `/passport/routes` → `RoutesFeed` (`src/passport/RoutesFeed.tsx`, route cards from `mapRoutes`; clicking a card highlights that route — see below), `/passport/stamps` → `PassportStamps` wrapped in `PassportPanel` (a scroll container mirroring PassportLayout's cream/texture bg + `max-w-3xl` padding + `pb-28`). Each view is conditionally rendered, so its hooks only run on its route. Deep-link `?category=`/`?neighborhood=` sync is gated to `/explore*` so other pages can't clobber in-session filters. `/events/:id` + `/routes/:id` + `/listing/:id` are marketing-layout detail pages; `/passport` (home) still uses PassportLayout (no pinned map).
- **Map routes** (`mapRoutes` in `src/data/sample-data.ts`): curated routes built from real listed spots. **Sponsored routes are authoritative from the partner Google Sheet (ROUTES tab)** — their `byTime.{morning,noon,night}` are the SAME fixed sheet order (not time-shuffled), and they carry two optional sheet-sourced fields: `description` (route-level start/transit note) and `stopNotes` (per-stop "Route Info" turn-by-turn guidance, keyed by business id). Every route carries `byTime: {morning, noon, night}` ordered `businessId` lists plus `starts: {marta, parking}` (older non-sponsored routes still vary stops/order by time of day). `resolveRoute(route, start, time)` picks the time-of-day stop list, reverses it when `start==="parking"`, computes per-leg haversine distances, and sizes the day to ~4–6 hours via **walking minutes + per-stop average visit time** (`visitMinutes` on a business, else `VISIT_MINUTES_BY_CATEGORY` default). It returns `miles` (summed from legs), `duration`, `legs[]`, and `visits[]` (per-stop minutes, rendered as `~Xm` in `RoutesFeed`). On `/passport/routes`, clicking a `RoutesFeed` card sets `selectedRouteId` in MapShell → the shared map shows ONLY that route's stops (ordered) and `BusinessMap` draws a yellow `RoutePath` polyline connecting them + fits bounds; clicking the active card again clears it (map shows all spots). With no route selected the map shows all businesses. `BusinessMap` accepts an optional `routePath?: {lat,lng}[]` prop.
- No FIFA branding anywhere — independent guide; disclaimer in footer.

## Passport stamp system

- **Schema** (`lib/db/src/schema/`): `visitors`, `businesses`, `stamps` (unique `(visitorId, businessId)` to prevent duplicates).
- **Server seeds** 6 sample businesses on boot (`artifacts/api-server/src/lib/seed.ts`) — idempotent via `onConflictDoNothing(slug)`. Push schema with `pnpm --filter @workspace/db run push`.
- **Endpoints**: `POST /api/visitors`, `GET /api/visitors/:id`, `GET /api/visitors/:id/stamps`, `GET /api/businesses`, `GET /api/businesses/:slug`, `POST /api/stamps` (returns `{stamp, alreadyCollected}`).
- **Frontend pages**: `/stamp/:slug` (auto-collects after passport exists; full-screen success), `/passport` (profile/home), `/passport/stamps` (filterable grid: All / Neighborhoods / Categories), `/passport/routes` (curated `mapRoutes` cards that highlight a route's stops on the shared map), `/admin/stamps` (QR + copy-link grid using qrserver.com).
- **Admin QR codes & the published domain**: `/admin/stamps` builds each stamp URL from `window.location.origin` — so QR codes generated from the private `*.replit.dev` workspace point to a Replit-gated URL that prompts visitors to log in / install the Replit app. The page detects a non-public host (`isPublicHostname`) and shows a **"Published site URL"** field: paste the live `.replit.app` (or custom) domain once (persisted in `localStorage` key `atlanta-passport-published-url`, validated to a public origin) and every QR code/link encodes that public origin instead — scannable with no Replit app/login. Stamp collection itself is public (`/stamp/:slug`) and only needs a Clerk passport account, not a Replit account. Admin/stamp pages use **DB** slugs (from `useListBusinesses`), which differ from the marketing `sample-data` ids.
- **Rewards page removed.** The `/passport/rewards` page and the My-Passport "Next reward" card were deleted. `REWARDS` data stays in `src/passport/data.ts` — still used by the My-Passport "Completed Passports" stat (count of total-stamp milestones reached).
- **Brand stamp graphic** (`src/passport/StampGraphic.tsx`): circular SVG with neighborhood arc-text, ATL center, lucide icon, EXPLORED + date arc. Color palette maps to brand tokens (yellow/red/lime/cream/navy/orange/sky). `locked` prop greys it out and swaps icon to Lock. Optional `iconUrl` prop renders a custom `<img>` in the stamp center (the per-business **Stamp Icon** from the partner sheet) instead of the lucide `Icon` — but only when present AND not `locked`. Consumers resolve the icon from `STAMP_IMAGE_BY_ID` / `STAMP_IMAGE_BY_SLUG` (in `sample-data.ts`): `stamps.tsx` + `StampChecklist.tsx` key by sample-data **id**, while `index.tsx` + `stamp.tsx` (DB-backed) key by **slug**.
- **Bottom nav** (`PassportLayout`): Explore · Routes · Events · Stamps · Contact · Profile (/passport).
- **Wouter v3 convention**: pass `className` directly on `<Link>`, never wrap with a child `<a>` (causes nested-anchor hydration error).
- **Codegen gotcha**: don't name an OpenAPI schema `XxxResponse` if there's an operation named `xxx` — orval generates an operation response constant with the same name, causing duplicate-export collision in `@workspace/api-zod`. (We use `StampCollection` for the `POST /stamps` body schema.)
- **Codegen gotcha 2**: orval-generated react-query hooks require explicit `queryKey: getXxxQueryKey(...)` inside the `query` options object (react-query v5 typing). Generated `XxxBody` zod schemas (not `XxxInput`) are the value exports; the `Input` names from the OpenAPI components only become TS types.

## Product

Marketing site with: Home, Become a Partner (Starter $100 / Featured $300 / Premier $500), Explore (filterable by category & neighborhood), Business Listing template (Wheelhaus Bikes as showcase), event/route/location detail pages, and Apply onboarding form. Visual identity is playful retro Americana — yellow block "ATL/PASSPORT" wordmark, thick black borders, offset "pop" shadows. (The standalone marketing About, Contact, Events-listing, and Beltline Tour pages were removed — see naming convention in User preferences.)

**Tourist-first UX:** Header has no Apply CTA (Explore instead). Mobile menu has a separated "For Businesses" section (cream block at bottom). Footer has a "For Businesses" mini-card on every page. Business CTA only appears once on home — at the bottom, presented as a curated/limited offer.

**Home page order (redesigned — "Field Guide / route-journey" sales tool):** Marquee → **Hero** (centered typographic "The Unofficial Field Guide to the Real Atlanta" with floating polaroid photos + an animated passport stamp; primary CTA → `/sign-up`; parallax via `useScroll`) → **Pillars** ("Your ticket to the city" — zigzag route-line layout of the 5 value props: Exclusive Deals, Match-Week Events, Zero Planning, Real Neighborhoods, Bike & Walk Routes) → **Journey** (navy horizontal-scroll cards of curated `routes`, showing stops/miles) → **Spots & Events** (orange grid mixing featured `events` + `businesses` with `offer` perks) → **Business CTA** (red block, "Get Listed" → `/apply`) → **Prizes** (`<PrizesSection/>` — last section before footer) → Footer. Each home section is its own component in `home.tsx` (`HeroSection`/`PillarsSection`/`JourneySection`/`SpotsAndEventsSection`/`BusinessCtaSection`); `PrizesSection` is a shared component (`src/components/PrizesSection.tsx`) rendered both here AND in `PassportLayout.tsx` (signed-in area, also directly before its Footer). All visible copy (incl. decorative stamp/polaroid labels) lives under `home.*` in the 9 locale files; pillar circle colors use an explicit `bg`/`fg` class map (never `bg-brand-${var}` — Tailwind can't see dynamic class names).

**Mobile safety:** `html/body/#root` have `overflow-x:hidden + max-width:100vw` to prevent rogue horizontal scroll from decorative offsets/stamps. `.button-pop` is `nowrap` with smaller padding/font (12px 18px / 0.82rem) on mobile and full size (14px 24px / 0.95rem) at sm+. Hero CTAs use `grid grid-cols-2 sm:flex` so two short-labeled buttons share a row on iPhone widths. Mobile navbar CTA is short ("Explore"); desktop nav uses "Start Exploring".

**Brand primitives (in `src/index.css`):** `.card-pop`, `.button-pop` (+`-yellow`/`-cream`/`-dark`), `.badge-sticker`, `.sticker-pill` + `.sticker-{yellow,red,cream,navy,lime,sky,orange}`, `.section-kicker`, `.highlight-yellow`, `.hero-title` (mobile clamp 2.25rem), `.passport-stamp`, `.texture-paper`, `.section-tight` / `.section-hero` (mobile-first spacing: 56px / 88px), `.shadow-pop`/`-sm`/`-lg`, `.dot-grid`, `.bg-paper`, `.route-line`, `.progress-track`/`.progress-fill`, `.form-progress-track`/`.form-progress-fill`, `.scroll-fade-r` + `.sm:no-fade` (right-edge mask for horizontal scrollers, disables on sm+). **Reusable React components:** `<Sticker color rotate icon>` and `<PassportStamp size tone rotate>`. Bungee display font; brand palette tokens (`brand-yellow/red/sky/navy/gold/lime/orange/cream` + `brand-yellow-foreground`). Marquee animates 22s mobile / 38s desktop.

**Page conventions:** Events cards use bold typographic date tiles (parsed Month + Day) instead of shared photos. Listing pages show a stamp preview + real Google Maps deep-link (no fake QR/scan UI), plus an optional **logo chip** overlaid top-right of the hero (the per-business Logo from the partner sheet; safe-cast `"logo" in biz ? biz.logo : undefined`, only rendered when present). Apply form has a sticky `top-16` progress strip showing live `completed/total` of required fields; package picker is 3 tiers (Starter/Featured/Premier) with a text-link "Request a custom package" that selects the `custom` enum.

**Apply form — Business vs Event toggle:** The Apply form (`src/pages/apply.tsx`) opens with a `submissionType` dropdown (`business` | `event`). `isEvent = watched.submissionType === "event"` drives everything: conditional labels (Event name/type/description vs Business name/Category/offer), event-only fields (`eventDate`*/`eventTime`/`eventVenue`*/`eventCost`/`eventUrl`), and hides business-only blocks (`nearMarta`/`nearBeltline`, subtitle, about, business hours, the package section 03, prize sponsorship). Validation is one `formSchema = z.object({...}).superRefine(...)` that branches on `submissionType` (business requires package; event requires eventDate/eventVenue; both require businessName/offer). The live progress `requiredFields` list also switches per type. `onSubmit` forces `package: "event"` for events. **Server** (`api-server/src/routes/applications.ts`): re-validates the branch server-side (400 if event missing date/venue, or business missing package) and **nulls out the other branch's fields on insert** so stale form state can't cross-contaminate; email subject/HTML/text rows are gated on `isEvent`. **Contract**: `submissionType` enum [business,event] + the five event fields live in `SubmitApplicationInput`/`Application`; `package` is optional in the spec (events have none — DB column stays notNull, server defaults it) and `submissionType` is required. DB schema (`lib/db/src/schema/applications.ts`) adds `submissionType` (notNull default 'business') + nullable `eventDate/eventTime/eventVenue/eventCost/eventUrl`.

## Internationalization (i18n)

- `i18next` + `react-i18next` + `i18next-browser-languagedetector` (LocalStorage key `atlanta-passport-lang`).
- 9 locales: en, es, fr, pt, de, it, ja, ko, ar — all under `src/i18n/locales/*.json`.
- Initialized in `src/i18n/index.ts`; imported by `main.tsx`. `applyDir()` strips region (`ar-SA` → `ar`) and sets `dir="rtl"`/`lang` on `<html>` for Arabic.
- Switcher: `<LanguageSwitcher variant="navbar"|"menu" />` (Globe + flag dropdown). Wired into `Navbar` (desktop + mobile menu).
- Translations regenerated via `scripts/translate-i18n.mjs <comma-list>` using the AI Anthropic integration. Brand names (Atlanta Passport, Beltline, Wheelhaus Bikes, World Cup, ATL, $100, TIER, etc.) preserved untranslated.
- Sample data in `src/data/sample-data.ts` (business names/descriptions/addresses/hours) is intentionally NOT translated — out of scope for the marketing site.
- RTL: use `rtl:rotate-180` on directional icons (ArrowRight/MoveRight); use `ltr:`/`rtl:` border/padding pairs where physical sides matter.

## User preferences

- **Site-structure / page naming convention.** The site is a fixed, curated set of pages — do NOT add new top-level pages without being asked. Canonical pages:
  - Passport (signed-in) — `/passport` = My Passport, `/passport/explore` = Explore, `/passport/routes` = Routes, `/passport/events` = Events, `/passport/stamps` = Stamps, `/passport/contact` = Contact.
  - Detail pages (one per selectable record) — `/listing/:id` = location details, `/events/:id` = event details, `/routes/:id` = route details.
  - Marketing / public — `/` = Home, `/partners`, `/apply`; plus `/sign-in`, `/sign-up`, `/stamp/:slug`, `/admin/*`.
  - Removed and should stay removed: marketing About, marketing Contact, marketing Events-listing, Beltline Tour, and the Rewards page.
- When changing site structure, only delete/repurpose pages that were explicitly requested; do not alter unrelated graphics or functionality.

## Gotchas

- **Always pair `lg:grid-cols-*` (or `md:`/`sm:`) with an explicit base `grid-cols-1`.** A grid with no column class at the current breakpoint defaults to a single auto-sized track that takes `max-content`, so any child requesting `max-w-2xl`/`max-w-6xl` will blow the grid wider than the viewport on mobile — overflowing past `overflow-x:hidden` clipping. The visible symptom is hero/CTA buttons sized like the viewport is 672px+ wide. Fixed in: hero + featured + CTA on home, hero + offers on beltline, listing main grid, partners pricing tiers.

## Pointers

- See the `pnpm-workspace` skill for workspace structure, TypeScript setup, and package details
